<h2>Tags</h2>
<ul><% @tags.each do |tag|%>
  <li><%= link_to tag.name, tag_path(tag), class: 'tag_name' %>   <%= link_to "Delete", tag_path(tag), method: :delete, data: {confirm: "For real?"} %><br>
  </li>
  <% end %>
</ul><br>
